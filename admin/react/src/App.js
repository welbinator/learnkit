/**
 * LearnKit Admin App Component
 * 
 * Root component for the LearnKit course builder interface.
 * Sprint 2: Full course builder with CRUD operations.
 * 
 * @package LearnKit
 * @since 0.2.0
 */

import CourseBuilder from './CourseBuilder';

function App() {
	return <CourseBuilder />;
}

export default App;
